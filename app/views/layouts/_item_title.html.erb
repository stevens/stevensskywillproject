<% if show_link %>
	<% item_type = type_for(item) %>
	<% case item_type 
		 when 'Match'
		 	 item_link_url = item_first_link(item)
		 else
		 	 item_link_url = item
		 end %>
	<%= link_to strip_tags(item.title), item_link_url %>
<% else %>
	<%= strip_tags(item.title) %>
<% end %>
