<div class="title"><h2><%= link_to h(item.title), recipe_url(item) %></h2></div>

<div class="basic">
	<ul>
		<li class="basic">
			<% if item.user == @current_user %>
	    		<%= "来自 " %><%= link_to "我", user_url(item.user) %>
	    	<% else %>
	    		<%= "来自 " %><%= link_to "#{item.user.login}", user_url(item.user) %>
	    	<% end %>
    		&nbsp;&nbsp;
    		<% if item.created_at == item.updated_at %>
    			<%= "#{item.created_at.strftime("%Y-%m-%d %H:%M:%S")} #{CREATE_CN}" %>
    		<% else %>
    			<%= "#{item.updated_at.strftime("%Y-%m-%d %H:%M:%S")} #{UPDATE_CN}" %>
    		<% end %>
		</li>
		<li class="basic">
			<% if item.tag_list.size > 0 %>
    			<%= "#{TAG_CN}: " %>					    			
    			<% 1.upto(item.tag_list.size) do |i| %>
	    			<%= link_to sanitize(item.tag_list[i-1]), { :action => :tag, :id => item.tag_list[i-1] } %>
	    		<% end %>
    		<% else %>
	    			<%= "#{TAG_CN}: 还没有#{TAG_CN}" %>
    		<% end %>
		</li>
	</ul>
</div>

<div class="main">
	<ul>
		<li class="text"><%= "#{PREP_TIME_CN}: #{time_display(item.prep_time, 'hm', HOUR_CN, MINUTE_CN, SECOND_CN)}" %></li>
		<li class="text"><%= "#{COOK_TIME_CN}: #{time_display(item.cook_time, 'hm', HOUR_CN, MINUTE_CN, SECOND_CN)}" %></li>
		<li class="text"><%= "#{DIFFICULTY_CN}: #{item.difficulty}" %></li>
		<li class="text"><%= "#{YIELD_CN}: #{sanitize item.yield}" %></li>
	</ul>
</div>

<% if show_todo %>
	<div class="todo">
		<% if item.user == @current_user %>
			<%= link_to "#{EDIT_CN}这#{UNIT_RECIPE_CN}#{RECIPE_CN}", edit_recipe_url(item) %> |
			<%= link_to "#{DELETE_CN}这#{UNIT_RECIPE_CN}#{RECIPE_CN}", recipe_url(item), :confirm => "确定要#{DELETE_CN}这#{UNIT_RECIPE_CN}#{RECIPE_CN}吗?", :method => :delete %>
		<% end %>
	</div>
<% end %>

<div class="main">
	<ul>
			<li class="label"><%= "#{DESCRIPTION_CN}:" %></li>
	</ul>
	<%= sanitize text_display(item.description) %>
</div>

<div class="main">
	<ul>
		<li class="label"><%= "#{INGREDIENT_CN}:" %></li>
	</ul>
	<%= sanitize text_display(item.ingredients) %>
</div>

<div class="main">
	<ul>
		<li class="label"><%= "#{DIRECTION_CN}:" %></li>
	</ul>
	<%= sanitize list_display(item.directions) %>
</div>

<div class="main">
	<ul>
		<li class="label"><%= "#{TIP_CN}:" %></li>
	</ul>
	<%= sanitize list_display(item.tips) %>
</div>