blog:
<% if (profile = user.profile) && !(blog = url_without_protocol(strip_tags(profile.blog))).blank? %>
  <% blog_url = url_with_protocol(blog) %>
  <span class='source'><%= link_to truncate(blog_url, 50), blog_url, :target => "_blank", :title => blog_url %></span>
<% elsif show_add && user == @current_user %>
  &nbsp;
  <span><%= link_to "#{ADD_CN}blog", :controller => 'settings', :action => 'account' %></span>
<% end %>